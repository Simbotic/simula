Behavior(
    "Loop Gate",
    Repeater((repeat:Forever)),
    [
        ("Delay", Delay((duration: 5.0)), [
            ("Can do?", Gate((script: Inline(r#"print(blackboard.state); blackboard.state = blackboard.state + 1; blackboard.state < 3"#))), [
                ("Do a thing", Debug((message: "I'm doing a thing!")))
            ])
        ]),
    ]
)